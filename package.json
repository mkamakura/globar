{
  "name": "globar",
  "description": "Run browserify and watchify with globs",
  "version": "0.10.0",
  "author": "Masaya Kamakura <kamakura.masaya@gmail.com> (https://github.com/mkamakura)",
  "bin": "bin/globar",
  "bugs": {
    "url": "https://github.com/mkamakura/globar/issues"
  },
  "dependencies": {
    "browserify": "^13.0.1",
    "browserify-incremental": "^3.1.1",
    "glob": "^7.0.3",
    "globby": "^4.1.0",
    "mkdirp": "^0.5.1",
    "touch": "^1.0.0",
    "watchify": "^3.7.0"
  },
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-eslint": "^6.0.4",
    "babel-preset-es2015": "^6.9.0",
    "chai": "^3.5.0",
    "eslint": "^2.11.1",
    "eslint-plugin-babel": "^3.2.0",
    "fixpack": "^2.3.1",
    "intelli-espower-loader": "^1.0.1",
    "istanbul": "^0.4.3",
    "mocha": "^2.5.3",
    "npm-run-all": "^2.3.0",
    "power-assert": "^1.4.1",
    "rimraf": "^2.5.2",
    "sinon": "^1.17.4",
    "version-bump-prompt": "^1.5.2"
  },
  "files": [
    "lib",
    "bin"
  ],
  "homepage": "https://github.com/mkamakura/globar#readme",
  "keywords": [
    "browserify",
    "cli",
    "glob",
    "watchify"
  ],
  "license": "MIT",
  "main": "bin/globar",
  "repository": {
    "type": "git",
    "url": "git+https://mkamakura@github.com/mkamakura/globar.git"
  },
  "scripts": {
    "build": "babel src --out-dir lib --source-maps inline",
    "bump": "bump --prompt --tag --push --all",
    "clean": "rimraf lib",
    "eslint": "eslint src ",
    "fixpack": "fixpack",
    "globar": "bin/globar \"tests/sample-project/src/**/index.js\" -o tests/sample-project/dist -inc -v",
    "istanbul": "istanbul cover _mocha --dir coverage/node -- --bail --recursive tests/fixtures tests/specs",
    "lint": "eslint src test",
    "release": "npm-run-all fixpack clean build eslint test bump",
    "test": "mocha --require intelli-espower-loader --bail --recursive tests/fixtures tests/specs",
    "watch": "babel src --out-dir lib --watch --source-maps"
  }
}